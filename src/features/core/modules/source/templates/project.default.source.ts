/** Пустой корневой граф нового проекта, готовый к запуску без внешних ресурсов. */
export const PROJECT_DEFAULT_SOURCE = `defineComposition({
  activateOn: startup(),
  data: {},
  resources: {},
  runtimes: {},
  hooks: [],
  outputs: {},
})
`
