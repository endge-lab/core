import type { EndgeMockProvider } from '@/domain/types/mock'

/** Built-in mock providers. Application-owned providers are registered at boot. */
export const ENDGE_CORE_MOCK_PROVIDERS: EndgeMockProvider[] = []
